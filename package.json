{
  "name": "auto-client-request",
  "scripts": {
    "setup": "cd frontend && npm i && cd ../backend && npm i",
    "backend": "cd backend && npm run start",
    "frontend": "cd frontend && npm run dev",
    "update": "cd frontend && npm run generate",
    "clean": "cd frontend && rm -rf node_modules && cd ../backend && rm -rf node_modules && cd services/platformatic && rm -rf dist && rm -rf node_modules"
  }
}
