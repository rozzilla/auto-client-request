{
  "name": "auto-client-request",
  "scripts": {
    "setup": "cd frontend && npm i && cd ../backend && npm i",
    "backend": "cd backend && npm run start",
    "frontend": "cd frontend && npm run dev",
    "update-frontend": "cd frontend && npm run generate",
    "force-full-clean": "cd frontend && rm -rf node_modules && cd ../backend && rm -rf node_modules && cd services/backend && rm -rf dist && rm -rf node_modules"
  }
}
